class DirectoryListing extends PathListing {
    colour = 128;
    backgroundColour = colours.red
}

function DirectoryListing:onMouseClick( event, handled, within )
    if not handled and within then
        self.plexusOwner:goToDirectory( self.path )
    end
end
